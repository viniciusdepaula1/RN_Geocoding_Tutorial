import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    position: 'absolute',
    padding: 13,
    elevation: 10,
    borderRadius: 25,
  },
});

export default styles;
